# Otahontas Tap

## How do I install these formulae?

`brew install otahontas/tap/<formula>`

Or `brew tap otahontas/tap` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
